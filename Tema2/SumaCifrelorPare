#include <iostream>
using namespace std;
int SumaCifrelorPare(int n) {
    int s = 0;
    while (n != 0)
    {
        if (n % 2 == 0)
            s = s + n % 10;
        n = n / 10;
    }
    return s;
}


int main() {
    int n;
    cin >> n;
    cout << SumaCifrelorPare(n);
    return 0;
}
