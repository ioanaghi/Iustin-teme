#include <iostream>
using namespace std;

int div_comuni(int a, int b) {
    int nr = 0;
    for (int i = 1; i <= a && i <= b; i++) {
        if (a % i == 0 && b % i == 0)
            nr++;
    }
    return nr;
}

int main() {
    cout << div_comuni(12, 18);
}
