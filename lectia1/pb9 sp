#include <iostream>
using namespace std;

int elimp(int n) {
    int r = 0, p = 1;
    while (n != 0) {
        int c = n % 10;
        if (c % 2 != 0) {
            r = r + c * p;
            p *= 10;
        }
        n /= 10;
    }
    return r;
}

int main() {
    cout << elimp(2457812);
}
